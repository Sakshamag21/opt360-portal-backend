# Base env — loaded for ALL environments by CRA (lowest priority).
# Environment-specific values live in .env.development / .env.staging / .env.production
# and always override these. Runtime Docker config (window._env_) overrides everything.

REACT_APP_WSO2_AUTHORITY=https://sso.uidai.net.in/oauth2
